# ops-collections 开发指导

本文档提供ops-collections库的开发环境构建、算子开发指南和测试说明。

## 一、环境构建

### 1.1 快速安装CANN软件

#### 安装前准备

离线安装时，请单击[获取链接](https://www.hiascend.com/developer/download/community/result?module=cann)下载CANN软件包，并上传到安装环境任意路径。

#### 安装CANN

请使用CANN9.0.0-beta.2及以上版本，其他版本暂不支持。

```sh
chmod +x Ascend-cann-toolkit_${VERSION}_linux-$(arch).run # 其中${VERSION}表示对应的CANN版本，如8.5.RC1
./Ascend-cann-toolkit_${VERSION}_linux-$(arch).run --install
```

#### 安装后配置

配置环境变量脚本set_env.sh，当前安装路径以${HOME}/Ascend为例。

```sh
source ${HOME}/Ascend/ascend-toolkit/set_env.sh
```

### 1.2 CANN详细安装指南

开发者可访问[昇腾文档-昇腾社区](https://www.hiascend.com/document)->CANN社区版->软件安装，查看CANN软件安装引导，根据机器环境、操作系统和业务场景选择后阅读详细安装步骤。

### 1.3 基础工具版本要求与安装

安装CANN之后，您可安装一些工具方便后续开发，参见以下内容：

* [CANN依赖列表](https://www.hiascend.com/document/detail/zh/CANNCommunityEdition/83RC1alpha002/softwareinst/instg/instg_0045.html?Mode=PmIns&InstallType=local&OS=Debian&Software=cannToolKit)
* [CANN安装后操作](https://www.hiascend.com/document/detail/zh/CANNCommunityEdition/83RC1alpha002/softwareinst/instg/instg_0094.html?Mode=PmIns&InstallType=local&OS=Debian&Software=cannToolKit)

## 二、项目架构

### 2.1 分层架构设计

ops-collections采用分层架构设计，从主机端到设备端分为多层：

```sh
┌─────────────────────────────────────────┐
│          应用层                          │
│    StaticMap / StaticMapRef             │
├─────────────────────────────────────────┤
│          实现层                          │
│    OpenAddressingImpl                   │
├─────────────────────────────────────────┤
│          组件层                          │
│  ProbingScheme | Storage | Hash         │
├─────────────────────────────────────────┤
│          基础层                          │
│    Pair | Extent | Allocator            │
└─────────────────────────────────────────┘
```

**架构说明：**

- **应用层**：提供容器类，StaticMap 为主机端容器，提供 Insert、Find、Erase 等算子接口；StaticMapRef 为设备端内部实现
- **实现层**：基于开放寻址法的哈希表实现
- **组件层**：探测策略、存储管理、哈希函数等可配置组件
- **基础层**：基础数据结构和工具类

### 2.2 目录结构

```sh
ops-collections
├── docs           //文档文件
├── doxygen        //Doxygen配置文件
├── include        //存放公共头文件
│   ├── detail     //容器实现细节
│   │   ├── extent              //extent具体实现
│   │   ├── hash_functions      //哈希函数实现
│   │   ├── open_addressing     //开放寻址法实现
│   │   ├── pair                //pair具体实现
│   │   ├── probing_scheme      //probing_scheme具体实现
│   │   ├── static_map          //static_map具体实现
│   │   ├── static_set          //static_set具体实现
│   │   └── storages            //存储相关实现
│   ├── utility                 //通用函数、工具
│   ├── bucket_storage.h        //BucketStorage容器头文件
│   ├── counter_storage.h       //CounterStorage容器头文件
│   ├── extent.h                //Extent头文件
│   ├── hash_functions.h        //哈希函数对外头文件
│   ├── macros.h                //宏定义
│   ├── pair.h                  //Pair头文件
│   ├── probing_scheme.h        //探测策略头文件
│   ├── static_map.h            //StaticMap容器对外头文件
│   ├── static_set.h            //StaticSet容器对外头文件
│   ├── static_map_ref.h        //StaticMapRef设备端引用头文件
│   ├── static_set_ref.h        //StaticSetRef设备端引用头文件
│   └── storage.h               //Storage基类头文件
├── scripts        //脚本文件存放目录
│   └── build.sh   //构建脚本
└── tests          //测试代码
    ├── common     //通用测试工具
    ├── performance//性能测试
    │   └── static_map
    ├── static_map //StaticMap单元测试
    └── utility    //基础组件测试
```

### 2.3 核心头文件说明

| 头文件 | 功能说明 |
|--------|---------|
| `static_map.h` | StaticMap容器对外头文件，提供主要的容器接口 |
| `static_map_ref.h` | StaticMapRef设备端引用头文件，用于NPU内部实现 |
| `static_set.h` | StaticSet容器对外头文件，提供主要的容器接口 |
| `static_set_ref.h` | StaticSetRef设备端引用头文件，用于NPU内部实现 |
| `detail/open_addressing/open_addressing_impl.h` | 开放寻址法实现，核心哈希表算法 |
| `detail/open_addressing/open_addressing_ref_impl.h` | 开放寻址法设备端实现 |
| `hash_functions.h` | 哈希函数集合 |
| `detail/open_addressing/kernels.h` | NPU kernel函数，设备端执行逻辑 |
| `pair.h` | 键值对结构定义 |
| `extent.h` | 容量表示类 |
| `storage.h` | 存储策略类 |
| `probing_scheme.h` | 探测策略定义 |

## 三、代码规范

### 3.1 命名规范

- 所有函数以大驼峰的方式命名
- 示例：
  - 错误: `add_test`
  - 正确: `AddTest`

### 3.2 代码风格

- 遵循C++11/14标准
- 使用模板编程实现泛型
- 使用constexpr提高编译期计算能力
- 使用noexcept标记不抛出异常的函数

## 四、测试指南

### 4.1 功能测试

#### 运行所有功能测试

```sh
bash scripts/build.sh -r
```

#### 运行单个测试

```sh
bash scripts/build.sh -r --test-name <test-name>
```

### 4.2 性能测试

#### 构建性能测试

```sh
bash scripts/build.sh -p
```

生成的性能测试执行文件在`/build/performance`文件夹下

#### 运行性能测试

```sh
bash scripts/build.sh -rp
```

### 4.3 测试工具

#### 通用测试工具

位于`tests/common/`目录：

| 工具 | 功能说明 |
|------|---------|
| `acl_env.h` | ACL环境初始化和清理 |
| `device_buffer.h` | 设备内存管理工具 |
| `dump_table.h` | 表内容导出工具 |
| `generators.h` | 测试数据生成器 |
| `golden.h` | 数据对比工具 |
| `map_factory.h` | 容器工厂类 |
| `matchers.h` | 测试匹配器 |
| `test_print.h` | 测试打印工具 |

## 五、算子开发实例

本节以StaticMap的Insert算子为例，详细介绍如何基于ops-collections开发算子，包括架构分析、开发步骤、测试方法等。

### 5.1 Insert算子架构分析

Insert算子采用分层架构设计，从主机端到设备端分为三层：

```sh
主机端API层 (StaticMap::Insert)
    │
    ├── 参数校验和初始化
    ├── Kernel启动配置
    └── 流同步和结果返回
            │
            ▼
Kernel入口层 (Insert Kernel)
    │
    ├── 线程索引计算
    ├── StaticMapRef构造
    └── 数据分发处理
            │
            ▼
设备端实现层 (StaticMapRef::Insert)
    │
    ├── 哈希计算
    ├── 探测策略
    ├── 原子操作CAS
    └── 冲突处理
```

### 5.2 开发步骤详解

#### 5.2.0 调用链概览

Insert算子的完整调用链如下：

```sh
用户代码
    │
    └─→ StaticMap::Insert (include/static_map.h)
            │
            └─→ OpenAddressingImpl::Insert (include/detail/open_addressing/open_addressing_impl.h)
                    │
                    └─→ InsertSimt Kernel (include/detail/open_addressing/kernels.h)
                            │
                            └─→ StaticMapRef::Insert (include/static_map_ref.h)
                                    │
                                    └─→ OpenAddressingRefImpl::Insert (include/detail/open_addressing/open_addressing_ref_impl.h)
```

**各层职责：**

- **StaticMap::Insert**：用户接口，提供类型安全的API
- **OpenAddressingImpl::Insert**：实现层，负责kernel启动和结果收集
- **InsertSimt Kernel**：NPU kernel入口，负责线程分配
- **StaticMapRef::Insert**：设备端引用，封装实现细节
- **OpenAddressingRefImpl::Insert**：核心实现，执行具体的插入逻辑

#### 5.2.1 步骤一：用户接口层（StaticMap::Insert）

用户接口层提供类型安全的API，将调用转发到实现层。

**代码位置：** `include/static_map.h`

**接口声明：**

```cpp
SizeType Insert(void *values, Extent valueNum, aclrtStream stream);
```

**实现代码：** `include/detail/static_map/static_map.inl`

```cpp
template <class Key,
          class T,
          class Extent,
          class KeyEqual,
          class ProbingScheme,
          class Storage>
typename StaticMap<Key, T, Extent, KeyEqual, ProbingScheme, Storage>::SizeType
StaticMap<Key, T, Extent, KeyEqual, ProbingScheme, Storage>::Insert(void *values, Extent valueNum, aclrtStream stream)
{
  return impl_->Insert(values, valueNum, stream);
}
```

**关键点：**

- 提供统一的用户接口
- 将调用转发到实现层`impl_`
- 保持类型安全和接口简洁

#### 5.2.2 步骤二：实现层（OpenAddressingImpl::Insert）

实现层负责参数校验、kernel启动和结果收集。

**代码位置：** `include/detail/open_addressing/open_addressing_impl.h`

**实现要点：**

```cpp
SizeType Insert(void *values, Extent valueNum, aclrtStream stream)
{
    // 1. 参数校验
    if (valueNum == 0) { return 0; }
    if (values == nullptr) { return valueNum; }
    
    // 2. 初始化失败计数器
    argStorage_.Initialize(0, stream);
    
    // 3. 获取AIV核数并启动kernel
    auto aivCoreNum = platform_ascendc::PlatformAscendCManager::GetInstance()->GetCoreNumAiv();
    aclco::Insert<KeyType, ValueType, bucketSize, ProbingScheme, KeyEqual>
        <<<aivCoreNum, 0, stream>>>(
        (uint8_t*)storage_.Data(),      // 哈希表指针
        (uint8_t*)values,                // 待插入数据指针
        (uint8_t*)emptyValueStorage_.Data(), // 空值指针
        storage_.Capacity(),             // 表容量
        valueNum,                        // 数据数量
        (uint8_t*)argStorage_.Data());   // 失败计数器
    
    // 4. 同步流并返回结果
    uint32_t ret = aclrtSynchronizeStream(stream);
    CheckRet(ret, "StaticMap::Insert::aclrtSynchronizeStream");
    return argStorage_.LoadToHost(stream);  
}
```

**关键点：**

- 参数校验确保输入合法性
- `argStorage_`用于统计插入失败的数量
- 使用AIV核进行并行计算
- 同步等待kernel执行完成

#### 5.2.3 步骤三：Kernel入口实现

Kernel入口负责线程分配和数据分发。

**代码位置：** `include/detail/open_addressing/kernels.h`

**实现要点：**

```cpp
template <typename Key, typename Value, uint32_t BucketSize,
          typename ProbingScheme, typename KeyEqual>
COLLECTION_SIMT_VF LAUNCH_BOUND(THREAD_NUM_LAUNCH_BOUND)
inline void InsertSimt(
  __gm__ uint8_t *table, __gm__ uint8_t *values, __gm__ uint8_t *emptyValue,
  uint32_t tableSize, uint32_t valueNum, __gm__ uint32_t *insertFailedNum)
{
    // 1. 计算全局线程索引
    uint32_t blockIndex = AscendC::Simt::GetBlockIdx();
    uint32_t blockNumber = AscendC::Simt::GetBlockNum();
    uint32_t globalThreadIdx = blockIndex * AscendC::Simt::GetThreadNum()
                               + AscendC::Simt::GetThreadIdx();
    uint32_t totalThreadNum = blockNumber * AscendC::Simt::GetThreadNum();

    // 2. 构造设备端引用
    using StorageRefType = aclco::BucketStorageRef<Value, BucketSize>;
    using ProbingSchemeType = ProbingScheme;

    StorageRefType tableRef = StorageRefType(tableSize, (__gm__ Value*)table);
    ProbingSchemeType probingScheme = {};
    KeyEqual predicate = {};
    StaticMapRef<Key, KeyEqual, ProbingSchemeType, StorageRefType> ref(
        *((__gm__ Value*)emptyValue), predicate, probingScheme, tableRef);

    // 3. 循环处理数据（每个线程处理多条数据）
    for (uint32_t i = globalThreadIdx; i < valueNum; i = i + totalThreadNum) {
        Value insertValue = *((__gm__ Value*)(values) + i);
        if (!ref.Insert(insertValue)) {
            AscendC::Simt::AtomicAdd(insertFailedNum, 1);
        }
    }
}

template <typename Key, typename Value, uint32_t BucketSize, 
          typename ProbingScheme, typename KeyEqual>
COLLECTION_AIV_GLOBAL void Insert(
    __gm__ uint8_t *table, __gm__ uint8_t *values,
    __gm__ uint8_t *emptyValue, uint32_t tableSize, uint32_t valueNum,
    __gm__ uint8_t *insertFailedNum)
{
    AscendC::Simt::VF_CALL<InsertSimt<Key, Value, BucketSize, ProbingScheme, KeyEqual>>(
        AscendC::Simt::Dim3{1024},
        table, values, emptyValue, tableSize, valueNum, 
        (__gm__ uint32_t*)insertFailedNum);
}
```

**关键点：**

- 使用SIMT编程模型，每个线程处理一个或多个数据项
- 通过`globalThreadIdx`和`totalThreadNum`实现数据分发
- 构造`StaticMapRef`对象用于设备端操作
- 使用原子操作统计失败次数

#### 5.2.4 步骤四：设备端引用层（StaticMapRef::Insert）

设备端引用层封装实现细节，提供简洁的设备端接口。

**代码位置：** `include/static_map_ref.h`

**接口声明：**

```cpp
template <typename Value>
COLLECTION_SIMT_DEVICE bool Insert(Value value) noexcept;
```

**实现代码：** `include/impl/static_map/static_map_ref.inl`

```cpp
template <typename Key,
          typename KeyEqual,
          typename ProbingScheme,
          typename StorageRef>
template <typename Value>
COLLECTION_SIMT_DEVICE bool StaticMapRef<
  Key,
  KeyEqual,
  ProbingScheme,
  StorageRef>::Insert(Value value) noexcept
{
  return impl_.Insert(value);
}
```

**关键点：**

- 提供简洁的设备端接口
- 将调用转发到核心实现`impl_`
- 封装底层实现细节

#### 5.2.5 步骤五：设备端核心实现（OpenAddressingRefImpl::Insert）

设备端实现负责具体的插入逻辑，包括哈希计算、探测策略、原子操作等。

**代码位置：** `include/detail/open_addressing/open_addressing_ref_impl.h`

**实现要点：**

```cpp
template <typename Value>
COLLECTION_SIMT_DEVICE bool Insert(Value value)
{
    __gm__ Value *tableHandle = storageRef_.Data();
    SizeType tableSize = storageRef_.Capacity();
    auto const key = this->ExtractKey(value);

    // 1. 创建探测迭代器
    auto probingIter = probingScheme_.template MakeIterator<bucketSize>(key, tableSize);
    auto const initIdx = *probingIter;

    // 2. 探测循环
    while (true) {
        __gm__ Value *bucketSlotsAddr = tableHandle + *probingIter;

        // 3. 遍历桶内的每个槽位
        for (size_t i = 0; i < bucketSize; i++) {
            auto const slotContent = *(bucketSlotsAddr + i);
            
            // 4. 检查键是否相等或槽位可用
            EqualResult insertFlag = predicate_.template operator()<IsInsert::YES>(
                key, this->ExtractKey(slotContent), this->ExtractKey(emptySlotValue_));
            
            if (insertFlag == EqualResult::EQUAL) {
                return false;  // 键已存在
            }
            
            if (insertFlag == EqualResult::AVAILABLE) {
                // 5. 尝试原子插入
                InsertResult result = AttemptInsert(
                    bucketSlotsAddr + i, emptySlotValue_, value);
                
                if (result == InsertResult::SUCCESS) {
                    return true;
                } else if (result == InsertResult::DUPLICATE) {
                    return false;
                }
                continue;
            }
        }
        
        // 6. 移动到下一个探测位置
        ++probingIter;
        if (*probingIter == initIdx) { 
            return false;  // 表已满
        }
    }
}
```

**关键点：**

- 使用探测策略解决哈希冲突
- 使用原子CAS操作保证线程安全
- 检查键的相等性和槽位可用性
- 处理插入成功、失败和重复的情况

**原子操作实现：**

```cpp
template <typename Value>
COLLECTION_SIMT_DEVICE InsertResult AttemptInsert(__gm__ Value *address,
                                             Value expected,
                                             Value desired) noexcept
{
    if constexpr (sizeof(Value) <= 8) {
        return PackCas(address, expected, desired);
    } else {
        return BackToBackCas(address, expected, desired);
    }
}

// 对于小于等于8字节的值，使用打包CAS
template <typename Value>
COLLECTION_SIMT_DEVICE InsertResult PackCas(__gm__ Value *address,
                                       Value expected,
                                       Value desired) noexcept
{
    using PackedType = typename Conditional<sizeof(Value) == 4, uint32_t, uint64_t>::Type;

    __gm__ auto *slotPtr = reinterpret_cast<__gm__ PackedType*>(address);
    auto *expectedPtr = reinterpret_cast<PackedType*>(&expected);
    auto *desirePtr = reinterpret_cast<PackedType*>(&desired);

    auto oldSlot = AscendC::Simt::AtomicCas(slotPtr, *expectedPtr, *desirePtr);
    auto *oldSlotPtr = reinterpret_cast<Value *>(&oldSlot);

    if (this->predicate_.EqualTo(this->ExtractKey(expected), 
                                  this->ExtractKey(*oldSlotPtr)) == EqualResult::EQUAL) {
        return InsertResult::SUCCESS;
    } else {
        if (this->predicate_.EqualTo(this->ExtractKey(desired), 
                                      this->ExtractKey(*oldSlotPtr)) == EqualResult::EQUAL) {
            return InsertResult::DUPLICATE;
        }
        return InsertResult::FAILED;
    }
}
```

### 5.3 测试方法

#### 5.3.1 单元测试

**代码位置：** `tests/static_map/insert_test.cpp`

**测试要点：**

```cpp
TEMPLATE_TEST_CASE_SIG(
  "static_map insert correctness",
  "[static_map][insert]",
  ((typename K, typename V, int BucketSize), K, V, BucketSize),
  (uint32_t, uint32_t, 1),
  (uint32_t, uint32_t, 5),
  (uint32_t, uint64_t, 5),
  (uint64_t, uint32_t, 1),
  (float, float, 1),
  (uint8_t, uint32_t, 1),
  (int8_t, int32_t, 1),
  (uint64_t, float, 1)) 
{
  // 1. 初始化ACL环境
  aclco::test::AclGlobalGuard g_acl;
  aclco::test::AclStreamGuard sg;
  auto stream = sg.stream;

  // 2. 创建测试数据
  auto hostPairs = aclco::test::MakeExamples<Key, Value>(seed, n, sent, "uniform", true);
  
  // 3. 执行插入操作
  aclco::test::DeviceBuffer<Pair<Key, Value>> dPairs(n);
  dPairs.CopyFromHostAsync(hostPairs.data(), n, stream);
  auto fail = map.Insert(static_cast<void*>(dPairs.Data()), 
                        aclco::Extent<std::size_t>(n), stream);
  
  // 4. 验证结果
  auto observed = aclco::test::DumpTable<Key, Value>(map, sent, stream);
  auto golden   = aclco::test::GoldenInsert<Key, Value>(hostPairs, sent);
  
  std::string diff;
  bool ok = aclco::test::EqualAsMap<Key, Value>(observed, golden, &diff);
  REQUIRE_PRINT(ok);
}
```

**测试覆盖：**

- 不同键值类型组合
- 不同桶大小
- 不同负载因子
- 边界情况（空表、满表、重复键）

#### 5.3.2 性能测试

**代码位置：** `tests/performance/static_map/perf_insert.cpp`

**测试指标：**

- 插入吞吐量（ops/s）
- 不同数据规模下的性能表现
- 不同负载因子下的性能表现

### 5.4 开发新算子的步骤总结

基于Insert算子的开发经验，开发新算子的步骤如下：

1. **需求分析**
   - 明确算子功能
   - 确定输入输出参数
   - 设计同步/异步接口

2. **架构设计**
   - 确定分层架构（用户接口层、实现层、Kernel层、设备端实现层）
   - 设计数据流和控制流
   - 确定并发策略

3. **主机端实现**
   - 在`StaticMap`类中添加接口声明
   - 在实现层添加具体实现
   - 处理参数校验和错误处理

4. **Kernel实现**
   - 编写Kernel入口函数
   - 实现线程分配逻辑
   - 构造设备端引用对象

5. **设备端实现**
   - 在`StaticMapRef`中添加设备端接口
   - 在`OpenAddressingRefImpl`中实现核心逻辑
   - 使用原子操作保证线程安全

6. **测试验证**
   - 编写单元测试
   - 编写性能测试
   - 验证正确性和性能

7. **文档编写**
   - 更新API文档
   - 添加使用示例
   - 编写开发说明

## 六、更多资源

- **[返回README](../README.md)**
- **[查看API文档](API文档和使用示例.md)**
- **[CANN社区版文档](https://www.hiascend.com/document/detail/zh/CANNCommunityEdition/83RC1alpha002/index/index.html)**
